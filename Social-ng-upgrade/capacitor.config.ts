import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Social-ng-upgrade',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
